linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - goimports
    - misspell
    - bodyclose
    - noctx
    - revive

linters-settings:
  goimports:
    local-prefixes: github.com/Pzhang768/Grimore-api

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
