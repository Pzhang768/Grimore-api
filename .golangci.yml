linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - goimports
    - misspell
    - bodyclose
    - noctx

linters-settings:
  goimports:
    local-prefixes: github.com/Pzhang768/Grimore-api

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
